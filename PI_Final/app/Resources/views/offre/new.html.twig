{% extends 'base.html.twig' %}

{% block container %}



    <h1>Offre creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" class="kafe-btn kafe-btn-mint-small full-width" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('offre_index') }}">Back to the list</a>
        </li>
    </ul>



{% endblock %}
